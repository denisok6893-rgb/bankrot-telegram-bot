# 🚀 Быстрый запуск бота с модулем "Карточки дел"

## ✅ Статус интеграции

### Что уже готово:

1. ✅ **bot.py обновлен**
   - RedisStorage для FSM (строка 1270-1275)
   - Импорты database и cases_handlers (строки 37-38)
   - Роутер зарегистрирован (строка 1278)
   - PostgreSQL инициализация добавлена (строка 3585)
   - Старая SQLite БД сохранена для совместимости

2. ✅ **Docker-окружение настроено**
   - PostgreSQL 16 с healthcheck
   - Redis 7 с persistence
   - Правильные зависимости между сервисами
   - Volume для данных

3. ✅ **Миграции готовы**
   - `alembic/versions/20260112_0000_001_create_cases_table.py`
   - Автоматический запуск при старте бота

4. ✅ **Модуль cases реализован**
   - CRUD хендлеры в `bankrot_bot/handlers/cases.py`
   - Модель Case в `bankrot_bot/models/case.py`
   - База данных в `bankrot_bot/database.py`

---

## 📋 Предварительные требования

### Необходимо установить:

- Docker >= 20.10
- Docker Compose >= 2.0 (или docker-compose >= 1.29)
- Git

### Проверка установки:

```bash
docker --version
docker compose version  # или docker-compose --version
```

---

## 🔧 Шаг 1: Настройка переменных окружения

### 1.1 Файл `.env` уже создан из `.env.example`

Отредактируйте `.env` и добавьте ваши данные:

```bash
nano .env
```

### 1.2 Обязательные параметры для изменения:

```env
# ⚠️ ОБЯЗАТЕЛЬНО: Получите токен от @BotFather в Telegram
BOT_TOKEN=your_telegram_bot_token_here

# ⚠️ ОБЯЗАТЕЛЬНО: Ваш Telegram ID (узнайте у @userinfobot)
ALLOWED_USERS=123456789,987654321
ADMIN_USERS=123456789

# Опционально: GigaChat (если используете)
GIGACHAT_AUTH_KEY=your_gigachat_auth_key
```

### 1.3 Параметры, которые можно оставить по умолчанию:

```env
# База данных (используется внутри Docker)
POSTGRES_DB=bankrot
POSTGRES_USER=bankrot
POSTGRES_PASSWORD=bankrot_password

# Redis (используется внутри Docker)
REDIS_URL=redis://redis:6379/0

# Режим работы бота
TELEGRAM_MODE=polling  # или webhook для продакшена
```

---

## 🚢 Шаг 2: Запуск Docker контейнеров

### 2.1 Запуск всех сервисов:

```bash
docker compose up -d
```

Или для старой версии docker-compose:

```bash
docker-compose up -d
```

### 2.2 Проверка статуса:

```bash
docker compose ps
```

**Ожидаемый вывод:**

```
NAME                IMAGE              STATUS
bankrot_postgres    postgres:16        Up (healthy)
bankrot_redis       redis:7-alpine     Up (healthy)
bankrot_bot         bankrot_bot        Up
bankrot_web         bankrot_bot        Up
```

### 2.3 Просмотр логов бота:

```bash
docker compose logs -f bankrot_bot
```

**Что должно быть в логах:**

```
✓ PostgreSQL database initialized
✓ Database tables created successfully
✓ Bot starting in POLLING mode
✓ Polling started
```

Для выхода из просмотра логов нажмите `Ctrl+C`.

---

## 🧪 Шаг 3: Проверка базы данных

### 3.1 Проверить подключение к PostgreSQL:

```bash
docker compose exec postgres psql -U bankrot -d bankrot -c "\conninfo"
```

**Ожидаемый вывод:**

```
You are connected to database "bankrot" as user "bankrot"...
```

### 3.2 Проверить таблицу cases:

```bash
docker compose exec postgres psql -U bankrot -d bankrot -c "\d cases"
```

**Должны быть колонки:**
- id, user_id, debtor_name, debtor_inn, case_number, court, stage, manager_name
- created_at, updated_at

### 3.3 Проверить Redis:

```bash
docker compose exec redis redis-cli ping
```

**Ожидаемый вывод:**

```
PONG
```

---

## 💬 Шаг 4: Тестирование в Telegram

### 4.1 Основные команды модуля:

Откройте вашего бота в Telegram и попробуйте:

#### Создать новое дело:

```
/newcase
```

Следуйте инструкциям бота, заполните:
- Код дела (например: ДЕЛО001)
- Номер дела (например: А40-123456/2024)

#### Посмотреть список дел:

```
/mycases
```

#### Просмотр конкретного дела:

```
/case 1
```

#### Установить активное дело:

```
/setactive 1
```

#### Редактировать дело:

```
/editcase
```

### 4.2 Проверка данных в БД:

После создания дела проверьте, что данные сохранились:

```bash
docker compose exec postgres psql -U bankrot -d bankrot -c "SELECT id, user_id, debtor_name, case_number FROM cases;"
```

---

## 🔍 Шаг 5: Проверка совместимости

### 5.1 Webhook endpoint (если используется):

```bash
curl http://localhost:8101/healthz
```

**Ожидаемый ответ:**

```json
{"status":"ok"}
```

### 5.2 Проверка существующих команд:

Убедитесь, что работают старые функции:
- `/start` - главное меню
- Генерация документов
- Профиль пользователя

---

## 📊 Шаг 6: Мониторинг

### 6.1 Проверить логи на ошибки:

```bash
# Логи бота
docker compose logs bankrot_bot | grep -i "error\|exception"

# Логи PostgreSQL
docker compose logs postgres | grep -i error

# Логи Redis
docker compose logs redis | grep -i error
```

### 6.2 Проверить подключения к БД:

```bash
docker compose exec postgres psql -U bankrot -d bankrot -c "SELECT count(*) FROM pg_stat_activity WHERE datname = 'bankrot';"
```

### 6.3 Проверить размер базы данных:

```bash
docker compose exec postgres psql -U bankrot -d bankrot -c "SELECT pg_size_pretty(pg_database_size('bankrot'));"
```

---

## 🛠 Troubleshooting

### Проблема: Бот не запускается

**Решение 1:** Проверьте переменные окружения

```bash
docker compose exec bankrot_bot env | grep -E "BOT_TOKEN|DATABASE_URL|REDIS_URL"
```

Убедитесь, что `BOT_TOKEN` не пустой.

**Решение 2:** Проверьте логи

```bash
docker compose logs --tail=50 bankrot_bot
```

**Решение 3:** Перезапустите контейнеры

```bash
docker compose down
docker compose up -d
```

### Проблема: Команды /newcase не работают

**Решение:** Проверьте, что роутер зарегистрирован

```bash
docker compose logs bankrot_bot | grep "cases_handlers"
```

### Проблема: База данных не инициализируется

**Решение:** Запустите миграции вручную

```bash
docker compose exec bankrot_bot python -m bankrot_bot.run_migrations
```

### Проблема: Redis не подключается

**Решение 1:** Проверьте статус Redis

```bash
docker compose ps redis
```

**Решение 2:** Проверьте REDIS_URL

```bash
docker compose exec bankrot_bot env | grep REDIS_URL
```

Должно быть: `redis://redis:6379/0`

---

## 🔄 Полезные команды

### Остановить бота:

```bash
docker compose stop
```

### Запустить бота:

```bash
docker compose start
```

### Перезапустить бота:

```bash
docker compose restart bankrot_bot
```

### Пересобрать образы:

```bash
docker compose down
docker compose build --no-cache
docker compose up -d
```

### Очистить всё (⚠️ удалит данные):

```bash
docker compose down -v
```

### Просмотр всех логов:

```bash
docker compose logs -f
```

### Зайти в контейнер бота:

```bash
docker compose exec bankrot_bot /bin/bash
```

---

## 📚 Дополнительная документация

- [VERIFICATION.md](./VERIFICATION.md) - Подробные команды проверки
- [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md) - Руководство по интеграции
- [README.md](./README.md) - Основная документация

---

## ✅ Контрольный список запуска

- [ ] Docker и Docker Compose установлены
- [ ] Файл `.env` настроен с вашим `BOT_TOKEN`
- [ ] `ALLOWED_USERS` содержит ваш Telegram ID
- [ ] Контейнеры запущены: `docker compose ps`
- [ ] PostgreSQL здоров (healthy)
- [ ] Redis здоров (healthy)
- [ ] Нет ошибок в логах: `docker compose logs bankrot_bot`
- [ ] Таблица `cases` создана в БД
- [ ] Команда `/start` работает в Telegram
- [ ] Команда `/newcase` работает
- [ ] Дело создаётся и сохраняется в БД
- [ ] Старый функционал не нарушен

---

## 🎉 Готово!

Если все пункты выполнены и бот отвечает на команды в Telegram - интеграция прошла успешно!

**Следующие шаги:**
1. Протестируйте все функции модуля карточек дел
2. Проверьте интеграцию с генерацией документов
3. Настройте webhook для продакшена (если нужно)
4. Настройте резервное копирование БД

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте раздел Troubleshooting выше
2. Посмотрите логи: `docker compose logs bankrot_bot`
3. Проверьте [VERIFICATION.md](./VERIFICATION.md) для детальной диагностики
