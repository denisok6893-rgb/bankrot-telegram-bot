# Bankrot Bot (@Bankrot_law_bot)

## Deploy (Timeweb)
1. cd /root/bankrot_bot
2. git pull origin main
3. docker compose -f docker-compose-simple.yml up -d
4. python3 -m venv venv
5. source venv/bin/activate && pip install -r requirements.txt
6. nohup venv/bin/python bot.py &

Phase 8: /newcase → Редактирование → /mycases → №6 → 📄 Документы → petition_6.docx ✓
