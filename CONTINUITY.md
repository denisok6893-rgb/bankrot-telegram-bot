# CONTINUITY.md

- Goal (incl. success criteria):
  - Сделать стабильное заполнение карточки дела и генерацию документов в Bankrot Telegram Bot (aiogram).
  - Success criteria:
    - При нажатии «📁 Заполнить карточку дела» бот начинает опрос **по порядку**, задавая следующий вопрос автоматически.
    - Ввод сохраняется в `case_cards.data` (SQLite), и после заполнения карточки документ «Заявление о банкротстве» генерируется без ошибки “missing”.
    - Нет дубликатов обработчиков/полей, поведение предсказуемое.

- Constraints / Assumptions:
  - Пользователь работает с телефона через Termux + SSH.
  - Не давать больше 2 шагов за раз; если нужен контроль — остановка на проверке.
  - Любые правки кода — только цельными логическими блоками (не “по одной строке”).
  - Бот запущен как systemd service: `bankrot-bot.service`.
  - Хранилище: `bankrot.db` (SQLite). CLI `sqlite3` может отсутствовать, проверка делается через python-скрипт.

- Key decisions:
  - Карточка дела хранится в таблице `case_cards`, поле `data` — JSON.
  - Авто-переход к следующему вопросу делается по списку обязательных полей (missing[0]).
  - В `state` хранится как минимум `card_case_id` и `card_field_key` (для текущего вопроса).
  - При генерации документов выбранное дело хранится в `state` как `docs_case_id`.

- State:
  - Branch: `petition-v1`
  - Bot работает, записи в `case_cards` сохраняются (проверено через python-запрос в SQLite).
  - UX проблема: после ответа на вопрос иногда показывается весь список полей/кнопок, создаётся ощущение “опять выдает весь список”.
  - В `bot.py` обнаруживались дубликаты обработчиков и структур:
    - несколько `@dp.callback_query(... case:cardfield: ...)`
    - несколько `@dp.message(CaseCardFill.waiting_value)`
    - несколько `CASE_CARD_FIELDS`
    - даже дубликаты `class CaseCardFill`
  - Это может приводить к непредсказуемому выбору обработчика и “лишним меню”.

- Done:
  - Исправлена компиляция (`python -m py_compile bot.py` проходит).
  - Проверено сохранение данных карточки в `bankrot.db` через python-скрипт (ROW EXISTS: True; ключевые поля заполнены).
  - Внесён коммит “Card fill: start asking first missing field” (уже запушен в `petition-v1`).

- Now:
  - Привести `bot.py` к единственной реализации карточки:
    - оставить один набор `CASE_CARD_FIELDS/CASE_CARD_FIELD_META`
    - оставить один `CaseCardFill`
    - оставить один обработчик `case:cardfield:*`
    - оставить один обработчик `@dp.message(CaseCardFill.waiting_value)`
  - Настроить поведение после сохранения ответа:
    - если есть missing → задавать следующий вопрос текстом (без вывода полного меню)
    - если missing нет → показывать итог и меню редактирования (кнопки полей — только по запросу)

- Next:
  - После стабилизации карточки: поправить генерацию документов так, чтобы:
    - “petition” проверял заполненность по `CASE_CARD_FIELD_META` (человеческие названия в ошибке)
    - “online hearing” тоже использовал выбранное дело из state при необходимости
  - Добавить кнопку “Продолжить заполнение” (если пользователь вышел на середине).

- Open questions (UNCONFIRMED if needed):
  - UNCONFIRMED: сейчас реально используется какой из дублирующих обработчиков — нужно подтвердить по актуальному `grep`/`nl` в `bot.py` после чистки.

- Working set (files/ids/commands):
  - Repo dir: `~/bankrot_bot`
  - Main file: `bot.py`
  - DB: `bankrot.db`
  - Service: `bankrot-bot.service`
  - Useful commands:
    - `python -m py_compile bot.py`
    - `sudo systemctl restart bankrot-bot.service`
    - Проверка карточки:
      - `python - <<'PY' ... PY` (sqlite3/json выборка из `case_cards`)
    - Поиск дублей:
      - `grep -n "case:cardfield:" -n bot.py`
      - `grep -n "@dp.message(CaseCardFill.waiting_value)" -n bot.py`
      - `grep -n "CASE_CARD_FIELDS = \\[" -n bot.py`
      - `grep -n "class CaseCardFill" -n bot.py`
